export default function Home() {
  return (
    <main style={{padding:32,fontFamily:'Inter, system-ui',maxWidth:900,margin:'0 auto'}}>
      <h1>Mosmak Library — Next.js starter</h1>
      <p style={{color:'#555'}}>This branch contains the Next.js + Tailwind scaffold for the Mosmak Library app. Next steps: connect Supabase, implement auth, and migrate the UI components.</p>
      <ul>
        <li>Branch: <strong>feature/nextjs-supabase-mvp</strong></li>
      </ul>
      <p>Run locally: <code>npm install</code> → <code>npm run dev</code></p>
    </main>
  )
}
